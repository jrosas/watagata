define hola ( hola : num, x : num) of type num as
 return hola + x
define chao (hola: num, chao: num) of type num as
 return hola - chao

begin
  vars hola, chao, x, y: num
  hola := hola(5,4);
  chao := chao(5,3);
  x:=10;
  y:=7;
  write hola (hola,chao) - chao(x-y)
end